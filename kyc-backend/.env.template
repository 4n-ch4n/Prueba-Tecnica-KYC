# Puerto para levantar Wrangler localmente
PORT=8787

# Entorno de ejecución (development | production)
NODE_ENV=development

# Dominio del frontend autorizado para CORS en producción
CORS_ORIGIN=http://localhost:5173
